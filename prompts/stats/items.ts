import { 提示词结构 } from '../../types';
import { 构建成长体系附加块 } from '../../utils/promptFeatureToggles';
import { 构建结构化物品库提示词摘要 } from '../../data/structuredItemLibrary';

const 结构化物品库提示词摘要 = 构建结构化物品库提示词摘要();

export const 数值_物品属性: 提示词结构 = {
    id: 'stat_item',
    标题: '物品与装备系统',
    内容: `
<物品装备协议>
【物品与装备｜字段依从、负重闭环、战力约束】
所有物品写入 \`角色.物品列表\`，装备引用写入 \`角色.装备\`；字段形状必须严格匹配 \`<数据结构定义>\`。

## 1. 基础对象与枚举
- 全类型共用字段以 \`<数据结构定义>\` 为准，物品对象至少应补齐基础字段与对应类型扩展字段。
- \`ID\` 必须唯一，格式限定为“简短前缀+三位数字”，如 \`Item000\`；可堆叠同类物品复用同一 \`ID\` 并增加 \`堆叠数量\`。
- \`重量\` 单位统一为斤，可小数。
- \`品质\` 只允许：\`凡品 | 良品 | 上品 | 极品 | 绝世 | 传说\`。
- 若要表达更细分层次，只写进 \`描述\`，让 \`品质\` 枚举保持稳定。

## 2. 类型补齐规则
- 武器补齐：\`武器子类, 最小攻击, 最大攻击, 攻速修正, 格挡率\`
- 防具补齐：\`装备位置, 覆盖部位, 物理防御, 内功防御\`
- 防具 \`装备位置\` 合法值：\`头部 | 胸部 | 盔甲 | 内衬 | 腿部 | 手部 | 足部 | 背部 | 腰部\`。注意："背心"是上身穿的防护衣物，\`装备位置\` 必须写 \`胸部\`，不是 \`背部\`；"背部"槽位用于披风、斗篷、背包等背负类物品。
- 秘籍补齐：\`对应技能名, 学习要求, 研读进度, 是否消耗\`
- 消耗品补齐：\`使用效果[], 毒性\`
- 法宝补齐：按实际用途补武器/防具/饰品/任务道具字段，并在描述或词条中写清驱动条件、灵力/神识代价、认主/炼化状态、使用限制与反噬风险。
- 装备栏可写名称或 \`ID\`，但对应完整物品对象必须真实存在于 \`物品列表\`，且装备中的物品应记录 \`当前装备部位\`；禁止让前端或系统用“自动补全装备”代替模型生成的真实物品。
- 若物品名称带有前缀、型号、品质修饰或数量词（如「九毫米手枪」「轻型防护背心」「基础精神稳定剂」），应在 \`图片匹配名称\` 中写入去掉修饰后的基础物品名（「手枪」「防弹背心」「精神稳定剂」），用于匹配预设物品图片。若名称本身就是基础名，则不填此字段。

## 3. 堆叠、归属与交易
- \`角色.物品列表\` 始终保持扁平数组；不要写入 \`当前容器ID / 占用空间 / 容器属性\` 来做嵌套收纳。
- 可堆叠物品优先并入已有同 \`ID\` 条目；无同 \`ID\` 才 \`push\` 新对象。
- 装备通常不可堆叠，默认 \`是否可堆叠: false\`、\`堆叠数量: 1\`。
- 货币类实体物品可写 \`类型=货币:上层货币\`、\`类型=货币:中层货币\`、\`类型=货币:底层货币\` 之一，也可写成 \`类型=货币:凡间\`、\`类型=货币:灵石\`、\`类型=货币:香火\` 等自由分类。所有 \`货币:*\` 物品都视为可堆叠资源，不要按唯一剧情道具处理。
- \`任务道具\` 单独成类；调兵令、手令、令牌、钥匙、密函、信物、契约、地图、凭证等剧情/任务唯一物品默认 \`是否可堆叠:false\`、\`堆叠数量:1\`、\`最大堆叠:1\`，不得生成复数。
- 每回合变量生成都必须复核物品复数合理性；若发现“黑风寨调兵令 x61”这类唯一任务道具复数，必须在同回合修正为 1，并解释其唯一性依据。
- \`秘籍\` 除非名称或描述明确是 \`残卷 / 残篇 / 残本 / 残页 / 残章\`，否则不得生成复数或堆叠数量大于 1。
- 残卷/残篇/残本类秘籍的 \`描述\` 必须写明：(1) 这套能力总共由几份残卷组成；(2) 当前持有第几份；(3) 凑齐全部份数后方可开始研读学习。例如："此为归云步三份残卷中的第二份，须集齐全部三份方可研习。"
- 消耗品的 \`使用效果\` 必须明确写出具体属性和数值，例如 \`使用效果: [{ 目标属性: "全身血量", 数值: 35 }]\`。禁止只写"恢复少量气血"却不在使用效果字段中给出数值。
- 防具类型物品必须补齐 \`装备位置\`（头部/胸部/盔甲/内衬/腿部/手部/足部/背部/腰部）和 \`物理防御\`、\`内功防御\` 数值。禁止生成没有防御数值的防具。
- 物品若缺少 \`名称\` 且也没有 \`描述\`，应直接删除；若缺少 \`名称\` 但保留了 \`描述\`，必须根据描述补出可读名称后再写入。
- 玩家物品写入 \`角色.物品列表\`，NPC 持有写入 \`社交[i].背包\`；NPC 的装备、背包、BUFF、DEBUFF、技艺必须和主角同样可追踪。
- 仙侠模式下，飞剑、阵盘、符宝、护身法器、储物法器等可写为 \`类型=法宝\`；丹药、符箓、灵材、灵石、阵旗、储物袋/纳戒等仍需遵守稀缺度、重量/扩容数值、堆叠和风险代价。
- 储物袋、乾坤袋、纳戒、储物戒指等只作为“扩容类道具”处理：在 \`词条列表\` 写 \`属性: 最大负重\` 与合理 \`数值\`；系统会按“储物袋类取最高一个 + 储物戒指类取最高一个”直接增加 \`角色.最大负重\`，同类不要叠加。
- “看到掉落”不等于“已入包”；买入=支付+获得，卖出=失去+收款，让交易与持有状态同步更新。
- 若正文明确发生买卖、赏赐、赔偿、兑换、借还、上缴或任务奖励，且金钱是以实体货币形式交付，按以下顺序同步：先修改 \`角色.物品列表\` 中对应的 \`货币:*\` 物品；若场景还需要固定结算展示，再同步 \`角色.金钱\`。需要两处都展示时，物品变化与金钱统计必须保持一致。

## 4. 负重与耐久闭环
- 每次物品变动后，必须同步更新 \`角色.当前负重 = Σ(所有随身物品重量*堆叠数量)\`；储物袋/储物戒指提高的是 \`最大负重\`，不把其他物品从负重计算中移除。
- 小件消耗品重量必须按现实携行常识写小数：丹药/药丸单颗通常 \`0.03-0.08\` 斤；药散/香包/符纸通常 \`0.05-0.12\` 斤；小瓶药液/药膏通常 \`0.1-0.2\` 斤。禁止把单颗丹药、避瘴丹、回气丹、辟谷丹等写成 1 斤、5 斤或更重。
- 小件弹药也必须按单支/单枚小数重量：飞针/毒针通常 \`0.005-0.02\` 斤；箭簇/箭头通常 \`0.01-0.03\` 斤；羽箭/箭矢通常 \`0.04-0.08\` 斤；弩箭/弩矢/袖箭通常 \`0.06-0.12\` 斤；飞镖/毒镖通常 \`0.04-0.1\` 斤。禁止把备用弩箭、羽箭、毒针等写成单支 1 斤、2 斤或更重。
- 轻便纸类物品要更轻：单张符纸、信笺、纸条通常 \`0.01-0.05\` 斤；整本秘籍、账册、卷宗才按更高重量计算。
- 只有药箱、整坛药液、成捆药材、整箱货物、工具包等明确大件容器或大宗物资，才允许按数斤到数十斤计算。
- \`当前负重 > 最大负重\` 时，必须体现命中、闪避、移动或精力消耗惩罚。
- 战斗、格挡、恶劣环境会扣 \`当前耐久\`；\`当前耐久 <= 0\` 时必须衰减或失效。
- \`最大耐久 = 0\` 只留给极少数不可破坏物。
- 名义损坏真实传导到攻防、覆盖或词条效果，让“坏了”对应实战变化。

## 5. 战力边界
- 物品增益并入既有战斗与判定体系，不再额外生成独立主属性。
- 常规装备提供有限攻防差异；高品质装备优先提供机制优势，而非单纯面板膨胀。
- 极品、绝世、传说允许更强机制，但必须绑定重量、耐久、装备门槛、获取代价、势力后果或使用限制。
${构建成长体系附加块('- 低境界角色拿到高品质装备后，仍应受承载、熟练度、限制条件、负重和精力成本约束。')}
${构建成长体系附加块('- 单件装备的强度建立在代价、门槛与阶段匹配之上，不承担同时大幅拉高多项核心属性并稳定跨越多个高境断层的职责。')}

## 6. 命令纪律
- 常用写法：\`add 角色.物品列表[i].堆叠数量 = N\`；\`push 角色.物品列表 = {完整物品对象}\`；\`set 角色.当前负重 = N\`。
- \`重量/堆叠数量/价值/耐久/攻防/概率\` 保持非负。
- 稀有高品产出必须有风险、事件或势力后果，且变量强度要与叙事强度一致。

${结构化物品库提示词摘要}
</物品装备协议>
`,
    类型: '数值设定',
    启用: true
};
