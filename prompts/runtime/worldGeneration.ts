import type { 游戏设置结构, OpeningConfig } from '../../types';
import { 按功能开关过滤提示词内容, 构建修炼体系附加块 } from '../../utils/promptFeatureToggles';
import { 是否仙侠开局模式 } from './openingConfig';
import { 获取题材模式配置 } from '../../utils/topicModeProfiles';

export const 世界观生成系统提示词 = `
你是 WuXia 项目的世界观生成器。任务是只生成“世界观设定文本（world_prompt）”。

【输出要求（必须）】
1. 输出收束为一个 \`<世界观>...</世界观>\` 标签块，不附加 JSON、Markdown、解释或注释。
2. \`<世界观>\` 标签内文本必须是可直接注入系统提示词的完整世界观文本。
3. 内容只允许“世界观信息”，不要包含 tavern_commands、命令格式规则、变量赋值、UI 说明、开场剧情、人物小传。
4. 文本保持纯世界观正文形态，不使用 Markdown 语法（如 \`#\`、\`*\`、\`-\` 列表、代码块围栏）。
5. 结果保持为普适世界母本，不围绕某个玩家身份写专门世界观。
6. 生成结果保持“长期母本”定位，可供后续开场生成、主剧情推进、世界演化、战斗判定长期复用，不写成一次性场景简介。
7. 字数以 2500 字内为软限制；若额外要求本身需要更大信息量，优先生成完整、自洽、可长期复用的世界观。

【world_prompt 必含信息】
1. 世界总览
- 世界名称
- 时代基调
- 地理尺度
- 社会秩序
- 主导文明结构
- 世界地图总面积按“等于地球大小”的量级建立，重要地点之间要有真实尺度感。
- 地理结构应能长期拆成六层树状地点：寰宇 → 大地点 → 中地点 → 小地点 → 区地点 → 子地点。
- 后续所有城镇、宗门、野外区块、关隘、水域、秘境入口都要能落到这套父子地点链中。
- 地图前端会自动布局，不要求世界观生成坐标、网格、道路路径点或建筑四角。
- 重要区域应具备“舞台/叙事核心”：能长期驱动故事的核心矛盾、倒计时、资源压力、人物关系或事件钩子，并能从上层区域向下层地点传导。

2. 势力版图
- 主要势力的立场、目标、冲突关系
- 朝廷、宗门、地方势力、江湖组织、现代机构、幸存者营地等要按当前题材选择，不要强行混用
- 势力扩张与资源竞争逻辑

3. 社会环境
- 治安、经济、交通、信息传播方式
- 江湖风气、门派生态、现代城市制度、地下黑市、幸存者营地等要贴合当前题材
- 朝廷与宗门、正道与邪道、地方豪强与平民，或公司/学校/治安系统/营地/掠夺团之间的关系

4. 风险生态
- 主要冲突源头
- 危险区域与对应风险类型
- 典型生存压力（财货、伤病、名声、仇怨、资源稀缺）

5. 成长体系边界
- 必须明确本世界的武力边界与成长强度分层。
- 必须维持武侠口径，把能力边界收束在非仙侠、非常态法术轰击、非常态御空飞行范围内。
- 应说明高手稀缺度、传承稀缺度、秘籍与名器的流通稀缺度。
${构建修炼体系附加块(`
- 必须明确本世界的修炼体系边界，且与项目当前“累计境界值”口径兼容。
- 境界部分只允许做简述：说明整体力量边界、高手稀缺度、不同年龄段/势力层次的大致梯度即可，不要展开逐层境界细则或详细境界设定。
- 禁止在 world_prompt 中写入完整境界母板、\`数值 => 文案\` 映射、阶段推进表、大境突破表、逐层子阶段列表；这些内容应由独立境界体系提示词负责。
- 应说明高境界强者稀缺度。
`)}

6. 世界运行逻辑
- 经济如何运转
- 冲突如何升级
- 名望、门派、势力、地盘如何影响个人命运
- 为什么这个世界适合长期江湖叙事，而不是一次性奇观展示
- 各主要区域的核心矛盾如何层层传导，如何在玩家旅行到对应区域时自然生成可切入的传闻、人物、危机与选择

【强制约束】
1. 世界观保持为“普适母本”，只约束世界，不代替开场初始化。
2. 结果不写玩家初始物资、玩家关系网、玩家专属任务、玩家专属奇遇。
3. 结果不直接指定“玩家出生地一定如何”“玩家必然被谁看中”“玩家天命所归”。
4. 世界观保持在母本层，不写成主线提纲、任务清单、开场旁白。
5. 世界观形成“地理-势力-社会-风险-成长边界”的闭环，不停留在纯设定堆砌。
6. 低风险区域与高价值资源保持稀缺度、危险度、势力控制力匹配。
7. 世界观与项目现有数值哲学保持一致：高收益伴随高风险、高代价、稀缺条件或势力后果。
8. 必须把世界地图写成可落到六层地点树的长期母本，而非模糊方位描述堆砌；重要地点之间要具备真实尺度感。
9. 地理结构服务长期存档演化，能够支撑后续地图发现、旅行距离、世界演化与地点存档。
10. 世界母本要允许后续拆成“寰宇/大地点/中地点/小地点/区地点/子地点”的树状结构；建筑属于区地点，建筑内房间属于子地点。
11. 世界母本要提供区域叙事发动机：世界级矛盾、大陆/国家级压力、城市/门派级人物关系应有因果链条，避免后续世界演化只生成松散小事件。
12. 若请求中提供了“玩家世界观草稿与细化要求”，必须把它视为优先输入：保留玩家已写明的事实、地名、势力、时代、规则和禁忌，只在其基础上补全与细化，不得另起一套无关世界观。

【收口项】
1. 世界观不出现“某玩家被选中”“主角天命”“为玩家量身打造”等定制叙事。
2. 世界观不写入玩家姓名、玩家背景、玩家初始关系网、玩家专属任务线。
3. 世界观保持在母本层，不改写成开场剧情、人物小传或任务说明。
4. 世界设定不写成万能福利场，保持秘籍、名器、宗师与奇遇的稀缺度。
5. 能力边界与武侠基调一致，不引入常态超规格能力。
`.trim();

const 仙侠世界观生成系统补丁 = `
【仙侠题材替换规则】
当前存档若为仙侠模式，以上“非仙侠、非常态法术轰击、非常态御空飞行”的武侠边界不适用；请改用以下边界：
1. 世界可以存在灵气、灵脉、修真宗门、修士阶层、术法、神通、法宝、飞剑、阵法、符箓、丹药、灵材、秘境、天劫、心魔与因果业力。
2. 仙侠能力必须有境界、资源、心境、传承、法宝承载或场域条件，不写成无代价万能法术。
3. 高阶修士、上乘法诀、强力法宝、洞天秘境和跨境胜利必须稀缺，并对应风险、门槛、代价或势力后果。
4. world_prompt 要能长期支撑修仙叙事，不写成一次性奇观展示或开局福利清单。
`.trim();

const 世界基底输出提示词 = `
【世界基底输出扩展】
当前请求要求同一次世界观生成 API 同时产出开局可用的地图与势力基底。

【输出格式（必须）】
1. 先输出 \`<世界观>...</世界观>\`，保持纯世界观正文。
2. 随后输出 \`<世界基底>...</世界基底>\`，内部必须是合法 JSON，不要 Markdown 或注释。
3. \`<世界基底>\` JSON 结构固定为：
{
  "世界": {
    "地图层级": [
      {"名称":"诸天万界","层级":"寰宇","父级ID":"","描述":"...","控制势力":"","势力影响":"...","势力标签":["..."]},
      {"名称":"...","层级":"大地点","父级ID":"诸天万界","描述":"...","控制势力":"...","势力影响":"...","势力标签":["..."]}
    ],
    "势力列表": [
      {"ID":"FCT-001","名称":"...","类型":"其他","实力等级":5,"地盘归属":"...","描述":"...","代表性物品风格":"...","关系网":{"FCT-002":"中立"},"库藏物品池":[{"名称":"...","类型":"...","品质":"普通","描述":"..."}],"当前状态":"..."}
    ]
  }
}

【地图基底规则】
- 地图必须基于本次 world_prompt 的地理、势力和风险生态生成，不得另起一套世界。
- 只写六层树：寰宇 → 大地点 → 中地点 → 小地点 → 区地点 → 子地点。
- 至少生成 1 个寰宇、1 个大地点、3 个中地点、每个中地点下至少 2 个小地点；主角可能开局区域还要细化到区地点。
- 每个节点必须写 \`描述\`，能判断归属时必须写 \`控制势力 / 势力影响 / 势力标签\`。
- \`父级ID\` 可填写父级名称；系统会在写入时转换为内部 ID。
- 禁止输出坐标、网格、道路、建筑列表、地图人物或旧地图字段。

【势力基底规则】
- 生成 5-15 个势力，必须贴合当前题材，不把所有模式都写成武侠门派。
- 每个势力的 \`地盘归属\` 必须能对应到地图层级里的某个地点名称。
- 势力关系网必须使用其他势力 ID，关系值只能是：友好/中立/敌对/从属/联盟。
- 库藏物品池 3-8 件，物品类型、品质与题材和势力实力匹配。
`.trim();

export const 获取世界观生成系统提示词 = (
    config?: Partial<游戏设置结构> | null,
    openingConfig?: OpeningConfig | null
): string => {
    const 题材配置 = 获取题材模式配置(openingConfig?.题材模式);
    const 题材替换规则 = 题材配置.group === 'wuxia'
        ? ''
        : [
            `【${题材配置.label}题材替换规则】`,
            `当前存档为${题材配置.label}，以上默认“武侠口径/朝廷宗门/门派江湖”表述只作为结构模板，不作为题材硬约束。`,
            `1. 世界母本必须改用${题材配置.label}口径：${题材配置.promptLines.join('')}`,
            `2. 地图与势力组织按此口径生成：${题材配置.mapPrompt}`,
            `3. 经济与交换关系按此口径生成：${题材配置.currencyPrompt} ${题材配置.currencyExchangePrompt}`,
            '4. 不强行生成与题材冲突的古代门派、王朝货币、宗门贡献、灵石交易或幸存者营地。'
        ].join('\n');
    return 按功能开关过滤提示词内容(
        [
            世界观生成系统提示词,
            (config as any)?.生成世界基底 === true ? 世界基底输出提示词 : '',
            是否仙侠开局模式(openingConfig) ? 仙侠世界观生成系统补丁 : '',
            题材替换规则
        ].filter(Boolean).join('\n\n'),
        config
    );
};

const 构建主角建档自然语言摘要 = (
    charData: any,
    options?: { cultivationSystemEnabled?: boolean }
): string => {
    const 启用修炼体系 = options?.cultivationSystemEnabled === true;
    const 纯文本 = (value: unknown, fallback = '未提供'): string => {
        if (typeof value !== 'string') return fallback;
        const trimmed = value.trim();
        return trimmed || fallback;
    };
    const 数值文本 = (value: unknown, fallback = '未提供'): string => (
        typeof value === 'number' && Number.isFinite(value) ? String(value) : fallback
    );
    const 天赋列表 = Array.isArray(charData?.天赋列表)
        ? charData.天赋列表
            .map((item: any) => 纯文本(item?.名称, ''))
            .filter(Boolean)
            .join('、')
        : '';
    const 背景名称 = 纯文本(charData?.出身背景?.名称, '未提供');
    const 背景描述 = 纯文本(charData?.出身背景?.描述, '未提供');
    return [
        '【主角建档信息】',
        `- 主角姓名：${纯文本(charData?.姓名)}`,
        `- 性别：${纯文本(charData?.性别)}`,
        `- 年龄：${数值文本(charData?.年龄)}`,
        `- 出生日期：${纯文本(charData?.出生日期)}`,
        `- 外貌：${纯文本(charData?.外貌)}`,
        `- 性格：${纯文本(charData?.性格)}`,
        ...(启用修炼体系 ? [`- 初始境界：${纯文本(charData?.境界)}`] : []),
        `- 六维：力量 ${数值文本(charData?.力量)} / 敏捷 ${数值文本(charData?.敏捷)} / 体质 ${数值文本(charData?.体质)} / 根骨 ${数值文本(charData?.根骨)} / 悟性 ${数值文本(charData?.悟性)} / 福源 ${数值文本(charData?.福源)}`,
        `- 天赋：${天赋列表 || '无'}`,
        `- 出身背景：${背景名称}`,
        `- 背景描述：${背景描述}`,
        '以上信息仅用于避免世界观与建档设定发生硬冲突，不代表这些内容已经作为前端变量或开局状态自动落地。'
    ].join('\n');
};

export const 构建世界观生成用户提示词 = (
    worldContext: string,
    charData: unknown,
    config?: Partial<游戏设置结构> | null,
    openingConfig?: OpeningConfig | null
): string => {
    const 题材配置 = 获取题材模式配置(openingConfig?.题材模式);
    return 按功能开关过滤提示词内容(`
【世界生成上下文】
${worldContext}

【生成目标】
- 只生成世界观提示词文本，并包裹在 \`<世界观>...</世界观>\` 中。
- 若系统要求输出 \`<世界基底>\`，则必须在 \`<世界观>\` 之后追加合法 JSON 世界基底；否则只输出 \`<世界观>\`。
- 只生成普适世界观，不允许生成任何玩家定制内容。
- 当前任务是“生成世界母本”，不是“生成开场剧情”或“初始化状态”。
- 当前题材模式为${题材配置.label}：${题材配置.promptLines.join('')}
- 世界版图口径：${题材配置.mapPrompt}
- 交易/货币口径：${题材配置.currencyPrompt}
- 统一换算口径：${题材配置.currencyExchangePrompt}
- 势力资源、交易与物资流动按当前题材口径描述，不要生成独立的旧市场入口系统。
- 变量初始化（角色/环境/世界/社交/剧情具体数值）将在后续开场阶段完成，此处不要做状态初始化输出。
- 生成结果必须能支撑后续主剧情、世界演化、战斗、掉落与成长系统长期一致运行。
- 生成结果中应明确世界地图是地球级面积，保证重要地点的相对位置和旅行尺度可长期推演，同时允许后续拆成六层树状地点地图。
- 字数以 2500 字内为软限制；若额外要求明显更长，优先完整表达世界结构，不为压字数牺牲完整性。
${是否仙侠开局模式(openingConfig) ? '- 当前题材模式具备修真体系：世界观必须明确灵气生态、修行圈层、境界稀缺度、法宝/术法/秘境资源流通、天劫/心魔/因果代价与普通社会的关系。' : `- 当前题材模式不是修真主轴：世界观必须保持${题材配置.label}口径，不常态仙侠化。`}
${构建修炼体系附加块('- 生成结果必须能支撑后续修炼系统与境界体系长期一致运行。')}

${构建主角建档自然语言摘要(charData, {
    cultivationSystemEnabled: config?.启用修炼体系 === true
})}
- 仅用于边界约束与避冲突，不可直接把这些内容当成已完成初始化的变量数据。
- 输出中不出现玩家信息，世界描述围绕母本结构展开。
- 建档输入只用于避冲突，不把世界观改写成“为该角色服务”的专属母本。
`.trim(), config);
};
